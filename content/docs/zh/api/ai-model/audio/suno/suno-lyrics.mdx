---
title: 生成歌词
full: true
---

- 根据主题或创作要求生成歌词，适用于歌曲创作前的歌词草拟。

- 本模型无版本维度，不要传 `version`；即使传入也会被 MAPI 丢弃，不影响计费。
- 任务完成后，结果数据中包含生成的歌词文本。
- 建议每 3–5 秒查询一次公共任务接口，直到任务成功或失败。

- **提交接口**：统一通过 `POST /v1/music/generations` 提交，使用 `model: "suno-lyrics"` 选择此能力。
- **任务查询**：提交返回 `task_id`，通过 `GET /v1/music/generations/{task_id}` 获取任务状态和结果。

<APIPage document={"openapi/generated/ai-model/音频（Audio）/Suno/post-v1-music-generations-suno-lyrics.json"} operations={[{"path":"/v1/music/generations","method":"post"}]} />
