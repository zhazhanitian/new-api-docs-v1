---
title: 提交异步图像生成任务
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: |
          提交图像生成任务，返回任务 ID，可通过 GET 接口轮询任务状态和结果。

          适用于生成时间较长（通常 30 秒以上）的图像模型，推荐用于生产环境。

          **支持的模型：** `gpt-image-2`

          **参考图（图生图）：** 通过 `image`（单张 URL/Base64）或 `images`（多张列表）字段传入参考图，即可触发图生图模式。`input_reference` 与 `image` 等价，任选其一即可。
---

{/* This file was generated by Fumadocs. Do not edit this file directly. Any changes should be made by running the generation command again. */}

提交图像生成任务，返回任务 ID，可通过 GET 接口轮询任务状态和结果。

适用于生成时间较长（通常 30 秒以上）的图像模型，推荐用于生产环境。

**支持的模型：** `gpt-image-2`

**参考图（图生图）：** 通过 `image`（单张 URL/Base64）或 `images`（多张列表）字段传入参考图，即可触发图生图模式。`input_reference` 与 `image` 等价，任选其一即可。

<APIPage document={"openapi/generated/ai-model/图像（Images）/异步图像任务/post-v1-image-tasks-submit.json"} operations={[{"path":"/v1/image-tasks/submit","method":"post"}]} />
