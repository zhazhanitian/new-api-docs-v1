---
title: 提交异步图像生成任务
full: true
_openapi:
  method: POST
  toc: []
  structuredData:
    headings: []
    contents:
      - content: |
          提交图像生成任务，立即返回任务 ID，生成在后台执行，可通过查询接口轮询状态和结果。

          入参格式与同步接口 `/v1/images/generations` 完全一致，可无缝切换。

          **参考图（图生图）：** 通过 `image` 字段传入，单张传字符串，多张传字符串数组，即可触发图生图模式。
---

提交图像生成任务，立即返回任务 ID，生成在后台执行，可通过查询接口轮询状态和结果。

入参格式与同步接口 `/v1/images/generations` 完全一致，可无缝切换。

<APIPage document={"openapi/generated/ai-model/图像（Images）/异步图像任务/post-v2-image-tasks.json"} operations={[{"path":"/v2/image-tasks","method":"post"}]} />
